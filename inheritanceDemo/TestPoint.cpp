/*
 * TestPoint.cpp
 *
 *  Created on: 14-Apr-2013
 *      Author: nirav.chotai
 */
//This is test driver file
#include <iostream>
#include "Point.h"
using namespace std;
/*int main(){

	//Instances
	Point point1;			//use default constructor
	Point point2(4, 5, 6);	//Invoke constructor

	point1.print();			//(0, 0, 0)
	point2.print();			//(4, 5, 6)

	//Object pointers, dynamic object allocation
	Point *ptrPoint1, *ptrPoint2;	//Declare 2 pointers
	ptrPoint1 = new Point;			//Dynamic memory allocation via new keyword

	ptrPoint2 = new Point(1, 2, 3);

	ptrPoint1->print();		// (0, 0, 0)
	ptrPoint2->print();		// (1, 2, 3)

	delete ptrPoint1;
	delete ptrPoint2;

	//Object References
	Point &refPoint1 = point1;
	refPoint1.print();		// (0, 0, 0)

	//Array of objects - static memory allocation
	Point point[3];
	point[0].print();	// (0, 0, 0)

	//Array of object pointers, dynamic memory allocation
	Point *ptrPoint[3];
	ptrPoint[0] = new Point(1, 2, 3);
	ptrPoint[1] = new Point(2, 3, 4);
	ptrPoint[2] = new Point(3, 4, 5);

	ptrPoint[0]->print();
	ptrPoint[1]->print();
	ptrPoint[2]->print();

	return 0;
} */


